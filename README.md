# shared
Shared metadata / files that are used in all Monitio repositories. This also includes the Monitio rules/guidelines/conducts.

`TODO(monitio/shared): Add the main stuff to monitio/shared instead of messing around because your tired.`
